import { Container } from 'react-bootstrap'

const Header = () => {
    return (
        <Container className="mw-100 bg-secondary text-center shadow">
            <h1 className="p-3 fw-bold font-monospace text-light">SuperHero App</h1>
        </Container>
    )
}

export default Header;